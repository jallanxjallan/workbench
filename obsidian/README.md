# Obsidian Layout

Workbench keeps the Obsidian surface split into two stable parts:

- `core/`: copied into a vault when provisioning it
- `control/`: shared content mounted into a vault as `_control`

Use this rule when adding new files:

- vault-local runtime files go in `core/`
- shared templates, queries, scripts, macros, and docs go in `control/`
