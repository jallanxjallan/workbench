#!/usr/bin/env zsh

# AutoScribe machine-level environment.
#
# Workbench deliberately avoids project-scoped exports here.
# Keep this file for non-content-specific shell bootstrap only.

export WORKBENCH_CONTENT_ROOT="${WORKBENCH_CONTENT_ROOT:-}"
export WORKBENCH_CONFIG_DIR="${WORKBENCH_CONFIG_DIR:-}"
export WORKBENCH_CACHE_DIR="${WORKBENCH_CACHE_DIR:-}"
