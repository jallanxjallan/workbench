---
slug: ""                  # auto-generated by your slug macro
status: 🖼️                # marks this note as an image reference or caption work
title: ""                 # short descriptive title
image: ""                 # relative path or URL
image_show: false         # toggle for inline display
filesize: ""              # optional, e.g. "3.1 MB"
dimensions: ""            # optional, e.g. "1920×1080"
photographer: ""          # optional credit line
date_taken: ""            # optional, YYYY-MM-DD
location: ""              # optional
caption: ""               # optional short caption for reference
tags: [image, passage]    # or however you categorize these
updated: <% tp.date.now("YYYY-MM-DD") %>
---

Brief description, contextual note, or draft caption.

---

```dataviewjs
const fm = dv.current();
if (fm?.image && fm.image_show === true) {
  dv.el("img", "", {
    src: fm.image,
    style: "max-width:560px;display:block;margin:0.75rem auto;"
  });
  if (fm.caption) {
    const cap = document.createElement("figcaption");
    cap.textContent = fm.caption;
    cap.style.fontSize = "0.9em";
    cap.style.opacity = "0.85";
    cap.style.textAlign = "center";
    cap.style.marginTop = "0.25rem";
    dv.container.appendChild(cap);
  }
}
```
