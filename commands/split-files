#!/home/jeremy/Python3.13Env/bin/python

import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[1]
ADAPTERS_PYTHON = REPO_ROOT / "adapters" / "python"
if str(ADAPTERS_PYTHON) not in sys.path:
    sys.path.insert(0, str(ADAPTERS_PYTHON))

from workbench.adapters.split_files import main

if __name__ == "__main__":
    raise SystemExit(main())
